{
  "name": "sjtu-login-addon",
  "version": "0.4.1",
  "private": true,
  "type": "module",
  "description": "Self-contained Chromium extension for filling SJTU jAccount captchas.",
  "license": "MIT",
  "scripts": {
    "build": "node build.mjs",
    "check": "node --test tests/*.test.mjs",
    "package": "npm run build && node scripts/package.mjs"
  },
  "dependencies": {
    "onnxruntime-web": "1.27.0"
  },
  "devDependencies": {
    "esbuild": "0.28.1"
  }
}
